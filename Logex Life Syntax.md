# Syntax for life

Let's create a good and bad character to show all combinations of logic:
- Jane - a good character.
- Ogre - a bad character.

This is mixed Laegna-Latin, because it does not use Laegna's own truth value symbols, but creatively uses Latin for Laegna semantics!

# Syntax

Let's use not or not not (checkmark) on left and right side - left side is local (causal, state) and right side is global (goal-based, goal). This means Logex is state-goal-based automation derived from Laegna Logecs - Laegna Logic and Math. "-ex" suffix in Laegna, in general, could mean automation especially if "cs" of science is replaced with "ex", sounding so similar that "U"-pronouncing rules are applied: if it's not so different, it might be hard to notice the difference in laegna, such as "U" and upside-down-U are pronounced similarly, such as some similar-meaning characters could look the same on first sight, or some fonts: probability is considered as meaningful and communicative.

## Jane is doing fine

The following are equivalent expressions to show Jane is good and should be doin good.

```prolog
Jane.
```

```prolog
✓Jane.
```

```prolog
✓Jane✓.
```

Syntactic sugar: ✓ could be opposite of not, emphasizing the yes - it's often confusing are you saying "not not x" to say it's not set that it's not true, or you say "x" where it's set that x is true - checkmark can avoid this, giving literal differences to thosem, Logecs-important factors as binary. ✓ could be used at empty places, to emphasize lack of ¬ visually, or to show it's logic.

```prolog
Jane = E.
```

This state Goal and State of Jane are True.
- Jane is doing well - True is an average of True, plus or minus.
- Jane should be doing well.
- Jane could be doing better, and this is what the Logex Machine will be now looking in combinations.
- A and E are goal to do well.
- O and E are state of doing well.

## Jane is not fine

The following are equivalent expressions to show Jane is not fine but should be doin good.

```prolog
Jane.
```

```prolog
✓Jane.
```

Syntactic sugar: ✓ could be opposite of not, emphasizing the yes - it's often confusing are you saying "not not x" to say it's not set that it's not true, or you say "x" where it's set that x is true - checkmark can avoid this, giving literal differences to thosem, Logecs-important factors as binary.

```prolog
Jane = E.
```

This state Goal and State of Jane are True.
- Jane is doing well - True is an average of True, plus or minus.
- Jane should be doing well.
- Jane could be doing better, and this is what the Logex Machine will be now looking in combinations.
- A and E are goal to do well.
- O and E are state of doing well.

The following are equivalent expressions to show Jane is good and should be doin g

```prolog
¬Jane.
```

```prolog
Jane = A.
```

This state Goal and State of Jane are True.
- Jane is doing well - True is an average of True, plus or minus.
- Jane should be doing well.
- Jane could be doing better, and this is what the Logex Machine will be now looking in combinations.
- A and E are goal to do well.
- I and A are state of not doing well.

## Ogre is fine but shouldn't be

Goal is set *after* the identifier (each sentence means the same):

```prolog
Ogre¬.
```

```prolog
✓Ogre¬.
```

```prolog
Ogre = O.
```

Here, going good would be bad..

This state Goal of Orge is False and State is True - O is local, and collapses in infinity.
- Ogre is doing well - True is an average of True, plus or minus.
- Ogre should be going bad.
- Ogre could be doing better, and this is what the Logex Machine will be now looking in combinations.
- I and O are goal to do bad.
- O and E are state of going well.

## Ogre is doing bad and falls in cage

But does it any help his enemies? This form of semantics is hardly used in Laegna and should not be associated with person, but their quality or negative achievement, such as "Ogre.Danger" where you rather fight with some danger, than person. But syntactically, you *can* set goal to false.

```prolog
¬Ogre¬.
```

```prolog
¬Ogre¬.
```

```prolog
Ogre = I.
```

Here, going good would be bad..

This state Goal of Orge is False and State is True - O is local, and collapses in infinity.
- Ogre is doing well - True is an average of True, plus or minus.
- Ogre should be going bad.
- Ogre could be doing better, and this is what the Logex Machine will be now looking in combinations.
- I and O are goal to do bad.
- I and A are state of doing bad.

# Jane helps Mary

Here, Jane helps Mary and should do so:

```prolog
Jane => Mary.
```

```prolog
(Jane => Mary) = E.
```

## Mary helps back

```prolog
Jane <=> Mary.
```

"<=>" uses symmetric "<=" and "=>", because the scope is singleton and they equal in same scope, whichever the real scope is.

```prolog
(Jane <=> Mary.)✓
```

It's here checked good, not assumed good.

# What to do with Goal-not?

Here I gave you discrete syntax where:
- If you put goal not at end of this syntax, you should ensure it's a negative term such as:
  - Sickness.
  - War or war loss, OA = material and IE = spiritual.
    - IE here, fits the spiritual term: not emotionally, magically or miraculously, breaking the natural laws - in Laegna Spiritual Reasoning, it met it tautologically; it's the moral loan left by goal-false war elements, and indeed IE measures this directly because it measures the class.

Outside this machine, what Goal-not means in Laegna:
- Simply existing the "not", it assumes it's still a *local sacrifice* for *global reason*, where local-global are connected to infinity lane geometry, and same syntax represents oneself-other but on ball geometry.

# Root and Crown

Yggdrasil: root, crown.
Chakras: root, crown.
Tao: yin, yang.

Whether opposed as binary-moeb (tao), seven-octave-division-into-higher-octave (Buddhism) or Z, X, Y (Viking Yggdrasil) it represents how person, with their connections to world, can metaphysically be seen - through frequencies.

Are they physically literal:
- This depends on coordinate system and material reality.
  - Imaginary forces which do beyond evolution and it's compressed syntax - evolved forms -, either in proto-evolution (matter stabilizes) or real evolution (matter accelerates, compression occurs - miracle of Life is born, and this is the real miracle: compression of evolutionary, long term into it's short, material form of here and now). We do in moments what those evolutions used to do in long trials before they achieved the fact - million year influence here, million there, as compressed fact of what we be and do now.
    - Mathematically, if any numbers are given, they mean what they mean by definition.
      - Laegna defines mathematical space there, and it's physical mappings are: long term influences in lower bands.
        - Physics model is incomplete, if no coordinates exist:
          - Therefore, if coordinates are defined, they exist. They are part of mathematical model to geometrically and numerically establish a parallel model to given reality.
          - Model works based on fundamental mathematical laws, rather than physics: mappings can occur between core causes, or their implications.

Basically: lower and higher band resonate with root and crown, material and heavenly frequencies:
- Crown is hologram cell reflecting whole reality, with it's unit inside creating half of it.
  - You are half.
  - Reality around is other half.
  - Because of ball-infinity correspondence, hologram cell maps a *perspective* of it.
  - This is Tao, Crown, but not Yang.
  - It's the total direction subjectively: Buddhist, Taoist statement that you are only effective in this subjective world - start from Crown, not God.
- Yang is the hologram head:
  - It answers the Kybalion definition of God. It might not be alive, but we use it anyway - it's a *total direction objectively*.
  - Tao and Yang in personal life are not God, but your flow in it.
  - It is objective because every entity is infinitesimal - in subjective view every *other* entity is infinitesimal, but you are mentally:
    - Central on your screen, very visible, and outwards projection fades off fast.
    - 1/16, 1/4 etc. relations could be used as well, they can be made number-consistent and interpolated.

You can find these crown and head in numbers:
- Crown, subjective whole, spirit symmetry - the global-band subset at your position, for example higher bands of digit values. Your digit positions are closes to you.
  - Buddhism, Taoism, Shamanism, Business and independence.
  - Instance-class calculus. Bottom-up. God inside.
- Totality, God, objective whole, soul symmetry - the whole global band, which might be brought down to the same precision. No digit is stronger than others.
  - Christianity, Conficianism, Civilization and common responsibility.
  - Class-instance calculus. Top-down. God outside.
 
Using bands, consistency of such relations can be shown, also whether they apply in particular case.

# Sincerity of A

A => A.

Because Laegna point and operation are *accelerative points*, "<=>", "=>", "<=" also show accelerative direction and if A, going on, results in accelerated A (in base calculation, acceleration dimension is not counted - it exists, in Laegna calculus, as mathematical law, not as something user is supposed to keep up, that coordinates are stable to accelerative laws is implied in that, coordinates are given and calculated in laegna, coherent math. It's not breaking up, following this law, so we *don't have to write out that acceleration occurs on implication, because it virtually occurs, and inside number, when we imply - the metaphysical, calculation time has passed and "activation" happened, where we project accelerated A into it's implication in system, but after the implication. We cannot pass the turing halting proof, liars paradox, berbers paradox otherwise: indeed, we *did* some calculation follows from calculation itself, because we cannot do it without leaving trace of it.
- This is what I mean: Latin syntax is *read from side*, Laegna is *read straight on* - instead of creating a still image of paradox and trying to empict it, we go the simple, Laegna way and produce the paradox itself - we imply. Implication is paradox because for real, the metaphysical time passed, for example we could "A => ¬A." - A implies not A. In Latin-based, base-2 logic it's paradox; in Laegna base-4 system it's logarithm: A is deceasing logarithmically, recursively, and we show the value of A on first integral, not plain number - zeroeth integral. On first integral, to travel from E to I when logic is made of accelerate point values, not ideal stills inside local ideal still, it's perfectly reasonable for first integral to be minus one or effective False (not value unset, but value False forced). We do not pass paradox - rather, we must do something with such acceleration, like avoid (escape), try to change the system (attack), or just live with it (stay still). Rather than biology classroom, we can use higher, spiritual values in Logex: we could help the value to do this kind of thing less, and maybe it becomes normal cityzen, lives in math realm. We can heal such unmet forces - this is why negative is positive.

We measure sincerity in that our soul is sure in it's value:
- In simulation, taotology etc., machine measures that the model has *actually* this value.
  - This can be estimation - probabilities allow this "empty forms" syntax because we do not associate it a value, but life - we flow consistently.

# Not goals in Real Laegna

If goal is not, the direction is still true.
- We have to show that the value *means* a negative aspect, such as person => not sickness.

Typically, Laegna associates:
- What it means for value to grow in IE terms - in long term, it's growth might overcome it's crime.
- What it means for value to grow in OA terms - in material, local, to gain values it seeks, where for us to avoid dangers we see in it, materially.

So if the whole "not person" as goal syntax is taken to Laegna for real, we see short and long mappings perfectly balance the syntax.

```prolog
Civilization.
(Ogre => not Civilization ∧ Ogre => not Mary) => (Ogre¬).
```

Notice Logecs uses componentiation:
- Logical syntax is followed in fragments, where it holds.
- I and E are used for long-term, where the fact comes back, as dimension - R, number position.
  - IOAE - I is at position I, O at position O, A at position A and E at position E, because we count laegna digits through same positions.
  - O and A positions will contain T: it's the fact you give.
  - I and E positions will contain not T: it's the opposite of your fact.
  - Componentwise, you apply Ogre not rule, but if you refer to this rule, you can do it directly through this implication.
  - For example, if Laegna Logecs uses three-digit "AAA" and "EEE", instead of "A" and "E", logic is applied in each digit - implication might be different.
    - Logical digits are rather meaning-associated, than strictly linearized.
      - This proves the syntax: in this syntax, balancing the system gives *faster* the case where Ogre is treated democratically, and along with spiritual values - Logecs machine cannot see only an enemy. *It sets excess qualities and possibilities offered to Ogre, now into Posetives - they are True, yet not wanted* - this is what you want.

`Ogre¬` - in Laegna, this gives Negotion, and *Negotion actually knows it's a negative fact*.
- Thus, statement which needs *no* must be anti-statement, and it's implication goes in reverse: but Logecs keeps head-up, and "as below so above" => you cannot change I to E and E to I, for example sickness=E should mean healing, sickness=I should mean sickness; this is Laegna polarization - if you want to do that, you set it to Latin normally, altough I do it often (Latin is the special, classic case in Laegna - but it uses it consistently, for example True and False are perfect for simple, small, absolutely consistent and perfect models, and we typically *can* write *True* and *False*, or equivalently *A* and *O* base-2).
- Negotion always builds two axes:
  - If you are poor:
    - One axe is by which you restrict yourself, and work. IE.
    - Other axe is by which you still enjoy, and use. OA.
    - They grow in different directions - restriction, growth.
  - If someone is criminal:
    - One axe is by which you restrict them, and for them it's IE, for you it might be OA.
    - Other axe is by which you heal them, helping them out in your own terms, and for them the result is OA and process thus, Z, for you the process is IE in Z.
    - Two axes grow differently - restriction, growth. Stability, growth.
    - For them, your positive axe might remain negative, tough - it's one interesting question about your IE, again mapping to typical spiritual axe, one that really exists for us.
  - In every case:
    - If you give up about your living conditions, and accept reality, your direction is still upwards.
      - What is true and false, then: two conditionals from two different logics? intuitively put to combinations?

So, Logecs syntax is very fit to model and idea life, also for simulations, automations, both metaphysics and precision - it also appears somewhat different in each, basic the systems on different things, like numbers, geometry, topology or logic. It tries to reuse what repeats, defining the same particulars and metaphors.

# What is metaphor?

Say A is class of people who like to Run.

A, thus, likes to Run - this is metaphor.

Mathematically, OA - people like to run - is first band, IE - their hologram likes to run - is second band, and if "likes to run" maps to material reality, it maps to it's class.

Symbols, poetic, metaphor language:
- Because many are mapped to one, it's mystical and reacts to itself in strange math.

Symbolism is spiritual - infinity-class syntax is real. Together, it's a real spirit: one you can model, argue, and prove. If you say people likes to run, indeed they belong to class which likes to run.

For example:

```prolog
Like_Run(every a in A) => Like_Run(class of A)
```

```prolog
Like_Run(every a in A except ones in B as well) => not class of B => Like_Run(class of A)
```

This is very mixed syntax - the last. It shows that now on, you do the math directly on classes:
- You do it directly with metaphor - but it becomes mystical, reacting, fractal timespace inside itself. This is the real infinity - a metaphor. This is a real human - an accelerating entity, unit in infinity. Acceleration, itself, is linear to infinity - life in this dimension is what grows upwards in it.

Thus, we have enough syntax for life, spiritual, metaphor speak:
- Metaphor contains war.
- We must assume, repeated / intentional reality must contain repeated war (directly material) or continuous hard-to-explain war (itself metaphor, cause-effect repetition).
  - It's natural that final class picture contains elements typical to horror movie, because relations of class are distilled, and such class-based projection in infinity becomes meaningfully real. In mathematical model world, analogous structures appear - the ones, which are repeated as it's causal chain members. Evolution takes care to separate these elements into class, into evolved form which does this freely and intentionally, based on their free will. (Does the selection itself have free will?)

# Aura visions

The metaphor appears:
- One sees higher dimension because
  - Nervous system maps coordinate coherence, which maps to higher dimension,
    - Visual cortex works on metaphors, and connects them multidimensional-infinitely.
      - We see causal chains in each case: if we map the proper connections.
      - Sensitive is wrong if they do not.
     
Test a sensitive:
- Give them semantically understandable image of repeated series.
- Is this somehow like "direct seeing" to see, compare, the infinity-correspondence directly as outer scope?
  - If yes, their image processing has semantically sensitive elements - they create visual correlation which maps to it sequentially, in relation to how it affects infinite, repeated games. This is the actual thing we are interested in - we can practically use.
 
Mathematical coordinates are always real, or not proven - they exist as model-correspondence. Metaphor and visual cortex are associated, and try to colocate the pixel element - brain system can efficiently, build metaphor. So can AI: so can an automata. And so can the simulation - one can show that metaphor is efficient, compressed image of actual causal chain, and if this is given information, it needs to appear inside geometric-emotional cortex, if geometry is used to relate, and color-like coding of emotional syntax to correlate goals and realities. Touch of meaninfulness, itself related to infinite repetition, is like base math - and we can see, human needs to experience something which looks like that base math. That is our spiritual basis - either it's associations, art, imagery, visualizations, even scientific, etc., it's *somehow a metaphor system to manage classes of real events and matters, basis*.

Inside this construction, we can discuss which qualities of such entities are real, modern, associative to real system entities, or which can destruct, obstruct, and disable vision.
